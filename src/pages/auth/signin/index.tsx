import React from "react";

import LoginModal from "../../../components/modals/LoginModal";

const SignIn = () => {
    return (
        <div className="loginPage w-screen h-screen flex items-center justify-center">
            <LoginModal />
        </div>
    );
};

export default SignIn;